{
    "id": 9,
    "title": "Writing Signup Logic",
    "description": "Accept a username and password and store it safely.",
    "content": "We’ll create a route that accepts user info and saves it with hashing.",
    "code_snippets": {
      "flask": { "language": "python", "code": "# in /signup POST: users[username] = generate_password_hash(password)" },
      "express": { "language": "javascript", "code": "// in /signup POST: users[username] = await bcrypt.hash(password, 10);" },
      "django": { "language": "python", "code": "# in /signup: users[username] = make_password(password)" }
    }
  }
  